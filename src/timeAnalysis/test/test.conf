#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Dijkstra test for any versions.',
        'data' => [ ],
        'tests' => [qw(
            doc-timeAnalysis
            )],
        'documentation' => [qw(
            doc-timeAnalysis
            )]
    },

);

1;
