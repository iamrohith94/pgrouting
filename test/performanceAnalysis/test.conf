#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Dijkstra test for any versions.',
        'data' => [ ],
        'tests' => [qw(
            doc-performanceAnalysis
            )],
        'documentation' => [qw(
            doc-performanceAnalysis
            )]
    },

);

1;
