#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Dijkstra test for any versions.',
        'data' => [ ],
        'tests' => [qw(
            doc-betweenness
            )],
        'documentation' => [qw(
            doc-betweenness
            )]
    },

);

1;
